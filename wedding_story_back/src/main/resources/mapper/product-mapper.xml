<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.product.model.dao.ProductDao">
  
  	<select id="totalCount" resultType="int">
  		select count(*) from product where board_status=1
  	</select>
  
  	<select id="selectProductList" resultType="board">
		select * from
			(select rownum as rnum, b.* from
    			(select 
        			product_no,
        			product_name,
        			product_thumb,
        			product_img,
        			product_price
    		from product where board_status=1 order by product_no desc) b)
		where rnum between #{start} and #{end}
	</select>
  
  	
</mapper>
