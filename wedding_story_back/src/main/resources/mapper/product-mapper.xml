<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.product.model.dao.ProductDao">
  <select id="TotalCount" resultType="int">
  	select count(*) from product
  </select>
  <select id="getProductList" resultType="product">
  select * from
		(select rownum as rnum, b.* from
		(select
		product_thumb,
		product_name,
		product_price
		from product order by product_no desc) b)
		where rnum between #{start} and#{end}
  </select>
  
  <insert id="insertProduct">
  	insert into product values(product_seq.nextval,#{companyNo},#{productName},#{productImg},#{productContent},#{productPrice},#{productThumb},#{coronation},#{diningRoom},#{numberPeople})
  </insert>
</mapper>
