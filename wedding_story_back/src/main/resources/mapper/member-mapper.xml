<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.member.model.dao.MemberDao">
	<select id="checkId" resultType="int">
		select count(*) from member where member_Id=#{checkId}
	</select>
	<select id="checkCode" resultType="int">
	select count(*) from member where company_no=#{checkCode}
	</select>
	<select id="checkLastMemberCode" resultType="int">
		SELECT MAX(SUBSTR((member_code), 2, 4)) FROM member
	</select>
	<select id="checkLastCompanyNo" resultType="int">
		SELECT MAX(SUBSTR((company_no), 2, 4)) FROM COMPANY
	</select>
	<select id="checkPartnerCode" resultType="String">
		select member_code from member where member_id=#{partnerId}
	</select>
	<insert id="insertMember">
	insert into member values(member_seq.nextval,#{companyNo},#{memberCode},#{memberType},#{memberId},#{memberPw},#{memberName},#{memberGender},#{memberPhone},#{memberEmail},#{partnerName},to_char(sysdate,'yyyy-mm-dd'),#{partnerId})
	</insert>
</mapper>
